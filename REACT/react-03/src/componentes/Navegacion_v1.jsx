import React from 'react';
import { useState } from 'react';

export const Navegacion_v1 = (props) => {
    const { navLinksData } = props;
    const [showSubMenu, setShowSubMenu] = useState([]);

    return (
        <nav>
            
        </nav>
    )
}
